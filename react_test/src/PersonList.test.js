// import { render, screen } from '@testing-library/react';

// // import with destructuring because it's not the default export
// import {PersonList} from './App'

// describe('PersonList', () => {
//   it('renders a PersonList', () => {
//     render(<PersonList/>);
//     const childElement = screen.getByText("Bob Smith");

//     expect(childElement).toBeInTheDocument();
//   })
// })